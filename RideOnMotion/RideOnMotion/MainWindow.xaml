<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RideOnMotion" x:Name="MainWindow1" x:Class="RideOnMotion.MainWindow"
        Title="RideOn[Motion]" Height="600" Width="800" Loaded="MainWindow_Loaded" MinWidth="800" MinHeight="600">
    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="75" Height="Auto" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="100" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" MinWidth="100" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>
        <StackPanel x:Name="DepthViewerPanel" MinHeight="150" Grid.Row="0" Grid.Column="0">
            <Viewbox>
                <local:DepthViewer/>
            </Viewbox>
        </StackPanel>

        <StackPanel x:Name="InfoPanel" Grid.Row="1" Grid.Column="0" Grid.RowSpan="2">
            <TextBlock Text="Insert info here" />
        </StackPanel>

        <GridSplitter Grid.Column="0" Width="3" Grid.RowSpan="3" />
        
        <StackPanel Background="DarkSlateGray" x:Name="MainPanel" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2">
            <Viewbox>
                <Image/>
            </Viewbox>
        </StackPanel>
        
        <StackPanel x:Name="LogPanel" VerticalAlignment="Top"  Grid.Row="2" Grid.Column="1">
            <Label Content="Insert log here"/>
        </StackPanel>
        
        <GridSplitter Grid.Row="2" Grid.Column="1" Height="3" VerticalAlignment="Top" HorizontalAlignment="Stretch" ResizeDirection="Rows" />
    </Grid>
</Window>
